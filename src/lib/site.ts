/* ---------------------------------------------------------------------------
 * SITE CONFIG
 *
 * Deliberately near-empty. Everything that used to live here was placeholder
 * text, and it has been removed rather than left in place to be overwritten
 * later — a plausible-looking wrong fact is worse than a missing one.
 *
 * Values get added back here only once Marie-Louise has supplied them.
 * ------------------------------------------------------------------------- */

export const site = {
  /** Full name, as it should appear on the page and in the browser tab. */
  name: "Marie-Louise Müller",
} as const;
